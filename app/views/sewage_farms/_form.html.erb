<%= form_for(@sewage_farm) do |f| %>
  <% if @sewage_farm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sewage_farm.errors.count, "error") %> prohibited this sewage_farm from being saved:</h2>

      <ul>
      <% @sewage_farm.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
