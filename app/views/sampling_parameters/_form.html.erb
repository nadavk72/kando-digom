<%= form_for(@sampling_parameter) do |f| %>
  <% if @sampling_parameter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sampling_parameter.errors.count, "error") %> prohibited this sampling_parameter from being saved:</h2>

      <ul>
      <% @sampling_parameter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
