<%= form_for(@sector) do |f| %>
  <% if @sector.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sector.errors.count, "error") %> prohibited this sector from being saved:</h2>

      <ul>
      <% @sector.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>


<%=select_tag 'sampling_parameters[]', options_for_select(
SamplingParameter.all.collect {|c| [ c.name, c.id ] }, 
@sector.sampling_parameters.map {|j| j.id}), :multiple => true, :class => "multiselect", :size => 10%>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
