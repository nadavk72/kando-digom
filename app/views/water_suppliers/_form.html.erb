<%= form_for(@water_supplier) do |f| %>
  <% if @water_supplier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@water_supplier.errors.count, "error") %> prohibited this water_supplier from being saved:</h2>

      <ul>
      <% @water_supplier.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
